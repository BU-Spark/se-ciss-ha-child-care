# Local dev: SQLite file (no Postgres install required)
DATABASE_URL="file:./dev.db"

# Production / team hosting (switch prisma/schema.prisma provider to postgresql first):
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/orientation_db?schema=public"
